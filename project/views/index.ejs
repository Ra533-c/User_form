<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white mx-auto">
            <form action="/create" method="POST">
                <input class="w-full rounded-md outline-none block px-5 py-2 bg-zinc-800 mt-2" type="text" placeholder="Title goes here" name="title">
                <textarea class="w-full rounded-md outline-none block px-5 py-2 bg-zinc-800 resize-none mt-2" name="details" id="" placeholder="write your tasks dets here"></textarea>
                <input class="mt-2 bg-blue-600 text-white hover:bg-blue-700 px-5 py-2 rounded-md font-bold" type="submit" value="Create Tasks">
            </form>
        </div>
        <div class="tasks p-10 flex gap-5 flex-wrap">
            <% if(files.length>0){ %>
                <% files.forEach((file)=>{ %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
                <h1 class="text-white text-3xl tracking-tighter mb-2"><%= file.replace('.txt' , '') %></h1>
                <div class="links flex w-full justify-between items-center">
                    <a class="text-zinc-600 hover:text-zinc-400" href="/edit/<%= file %>">Edit</a>
                    <a class="text-blue-500 hover:text-blue-400" href="/read/<%= file %>">Read More →</a>
                </div>
                    </div>
                <% }) %>
            <% }else { %>
            <h3 class="text-zinc-600">no tasks yet.</h3>
            <% } %>
        </div>
    </div>
</body>
</html>